{
  "name": "volunteermanager",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build": "esbuild app/javascript/**/*.js --bundle --sourcemap --outdir=app/assets/builds",
    "build:watch": "esbuild app/javascript/**/*.js --bundle --sourcemap --outdir=app/assets/builds --watch"
  },
  "devDependencies": {
    "esbuild": "^0.17.19"
  },
  "dependencies": {
    "@rails/actioncable": "^8.0.200",
    "@rails/activestorage": "^8.0.200",
    "@rails/ujs": "^7.1.3-4"
  }
}
